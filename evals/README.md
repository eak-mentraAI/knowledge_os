# Evals

Evaluation suites validate extraction quality, canonicalization accuracy, edge precision, retrieval groundedness, hallucination rate, context-pack usefulness, project-space usefulness, and runtime export completeness.

This directory may be empty only while no evaluated behavior exists.

