# Integration Tests

Integration tests validate behavior across services, packages, adapters, and persistence boundaries.

This directory may be empty only while no cross-boundary implementation exists.

