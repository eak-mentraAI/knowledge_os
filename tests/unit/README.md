# Unit Tests

Unit tests validate pure functions, models, mappers, prompt builders, parsers, policy checks, and UI components.

Every implemented unit must have corresponding unit coverage unless a sprint ledger note documents why another test layer owns the proof.

