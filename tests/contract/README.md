# Contract Tests

Contract tests validate versioned schemas, API payloads, adapter interfaces, generated artifacts, runtime export packages, and cross-package boundaries.

This directory may be empty only while no implementation files exist in the contract scope.

