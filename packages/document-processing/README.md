# Document Processing

Document processing converts source records and extracted source content into canonical documents.

Current sprint scope:

- Controlled HTML to canonical document generation.
- Stable document and section IDs.
- Evidence spans over normalized document text.
- Link and simple table extraction for fixtures.

Out of scope until later sprints:

- Browser rendering.
- PDF extraction.
- Markdown repository parsing.
- Advanced table structure recovery.

