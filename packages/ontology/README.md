# Ontology

The ontology package exposes the domain-agnostic Knowledge OS base ontology.

Current sprint scope:

- Load the base ontology.
- List and find node and edge types.
- Guard against duplicate base definitions.

Foundry-specific domain pack behavior starts in W2-S2.

