# Source Acquisition Contracts

This package exposes the W1-S1 source acquisition schema registry for tests and future implementation packages.

It intentionally contains no crawler behavior. Crawling begins in later W1 sprints.

